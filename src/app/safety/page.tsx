import { InfoPage } from "@/components/info-page";

export default function SafetyPage() {
  return (
    <InfoPage eyebrow="Support" title="Safety tips">
      <p>Meet in a public, well-lit place and tell someone where you are going. Avoid private homes for first-time swaps.</p>
      <p>Inspect items before completing the swap. If something feels off, pause the exchange and report the listing or message.</p>
    </InfoPage>
  );
}
