import { InfoPage } from "@/components/info-page";

export default function CookiesPage() {
  return (
    <InfoPage eyebrow="Legal" title="Cookie policy">
      <p>SwapShelf uses essential cookies for authentication, session refresh, and account security.</p>
      <p>These cookies keep you signed in and help protect dashboard, admin, offer, and messaging routes.</p>
    </InfoPage>
  );
}
