import { InfoPage } from "@/components/info-page";

export default function ContactPage() {
  return (
    <InfoPage eyebrow="Support" title="Contact us">
      <p>For account help, moderation questions, or safety concerns, email support@swapshelf.local from the email address on your account.</p>
      <p>Include listing, offer, or message details when possible so the team can review the issue faster.</p>
    </InfoPage>
  );
}
