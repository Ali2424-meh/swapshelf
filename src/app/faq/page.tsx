import { InfoPage } from "@/components/info-page";

export default function FAQPage() {
  return (
    <InfoPage eyebrow="Support" title="Frequently asked questions">
      <p>SwapShelf helps nearby members trade items directly. Browse listings, send an offer, then use messages to agree on meetup details.</p>
      <p>No payments are handled by SwapShelf. Inspect items in person and only complete a swap when both sides are satisfied.</p>
    </InfoPage>
  );
}
